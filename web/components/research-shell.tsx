type Props = { children: React.ReactNode; view: "research" | "history"; signedIn: boolean; onNavigate: (view: "research" | "history") => void; onAccount: () => void; onMigration: () => void };

export function ResearchShell({ children, view, signedIn, onNavigate, onAccount, onMigration }: Props): React.ReactElement {
  return <main className="site-shell"><header className="topbar"><button className="wordmark" type="button" onClick={() => onNavigate("research")}><span className="logo-mark">R</span><span>ResearchAssistant</span></button><nav aria-label="Main navigation"><button className={view === "research" ? "nav-link active" : "nav-link"} type="button" onClick={() => onNavigate("research")}>Research</button><button className={view === "history" ? "nav-link active" : "nav-link"} type="button" onClick={() => onNavigate("history")}>Archive</button></nav><div className="header-actions"><button type="button" className="migration-link" onClick={onMigration}>Migrate history</button><button type="button" className="account-button" onClick={onAccount}><span className={signedIn ? "status-dot ready" : "status-dot"} />{signedIn ? "Account" : "Sign in"}</button></div></header>{children}<footer className="footer-note">Evidence first <span>·</span> private by account <span>·</span> inspectable by design</footer></main>;
}
